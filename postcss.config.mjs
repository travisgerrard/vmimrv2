// Use CommonJS syntax for compatibility with older setups if needed,
// but ES Modules should work fine with modern Node/Next.js.
const config = {
  plugins: {
    // Use object syntax for plugins
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
